# gcp-composer-compat-validator
A utility to validate Python package compatibility with Google Cloud Composer environments before deployment. It helps prevent dependency conflicts and environment update failures by testing against official Composer images in a local Docker container.
